steps:   
    # build the app image
    - name: 'gcr.io/cloud-builders/docker'
      args: ['build', '-t', 'eu.gcr.io/krisenheldin/api', '.']

# image is pushed to Container Registry
images:
- 'eu.gcr.io/krisenheldin/api'